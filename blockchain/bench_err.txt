
  ❌ IPFS node is NOT running!
  ─────────────────────────────────────────────────────
  IPFS is a HARD REQUIREMENT for this benchmark.
  Start the IPFS Docker container with:

    docker run -d --name ipfs_node \
      -p 5001:5001 -p 8080:8080 \
      ipfs/kubo

  Then re-run this benchmark.
  ─────────────────────────────────────────────────────
Assertion failed: !(handle->flags & UV_HANDLE_CLOSING), file src\win\async.c, line 76
